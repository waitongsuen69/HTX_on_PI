# Server
PORT=8080
BIND_ADDR=0.0.0.0
REF_FIAT=USD
PULL_INTERVAL_MS=60000

# Optional runtime flags
# Set to 1/true to seed a sample snapshot and skip HTX calls
DRY_RUN=0
# Set to 1/true to skip opening a network port (for restricted envs)
NO_LISTEN=0

# Backfill history on boot (days)
BACKFILL_DAYS=180

# Auto-open browser on server start (1/0)
AUTO_OPEN=1

# Optional: override default browser command (Linux)
# BROWSER=chromium-browser

# Debug logging (optional)
# Set to 1/true to print per-account balance merge details
DEBUG=0

# Tron on-chain (optional)
# Public full node or your own
TRON_FULLNODE=https://api.trongrid.io
# (No additional TRON flags; balances are read directly from the chain.)

# Cardano on-chain (optional)
# Provider: currently only Blockfrost is supported
CARDANO_PROVIDER=blockfrost